import React from 'react';
import UserTemplate from 'templates/UserTemplate';

const Cennik = () => (
  <div>
    <UserTemplate>
      <h1>Cennik</h1>
    </UserTemplate>
  </div>
);

export default Cennik;
